{
  "name": "workerd-subcommand-separation",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "register": "tsc && node --env-file=.env dist/register.js",
    "deploy": "wrangler deploy --minify",
    "log": "wrangler tail",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "discord-api-types": "^0.38.54",
    "typescript": "^7.0.2",
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "discord-hono": "^0.22.1"
  }
}
